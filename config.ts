// @TODO @FUTURE .env files are not working and are ignored by AI - figure this out later, not important now

export const config = {
  SERVER_PORT: 3069,
  SERVER_API_URL: 'http://localhost:3069',
  MONGODB_URI: 'mongodb://admin:123456@localhost:27017/admin',
  MONGODB_DB: 'admin',
};