{
  "name": "littlejs-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev": "live-server --port=3069 --no-css-inject --watch=src,index.html --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "3.6.2"
  },
  "dependencies": {
    "littlejsengine": "^1.11.10"
  }
}
